# selenium4paper
based on selenium and python, crawling pages for their references and by which it has been sited
# modules.py
usually paper websites have different methods to storage information about paper references and citings, so I created two classes accordingly to suit differnet needs for processing and storaging data

# tool.py
contains functions for retrieving info like paper titles, authors, references, citings  
main function for retrieval like `get_all_cited_nodes()` and `get_all_reference_nodes()`

# run.py
pls carefully read what tasks can do what before operating, the first four have been tested to be solid

# root paper
Highly accurate protein prediction with Alphafold
